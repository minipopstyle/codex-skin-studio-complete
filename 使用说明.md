# Codex Skin Studio Complete 使用说明

## 一、引擎和 Skill 的关系

Skill 不直接修改 Codex。它们负责处理素材、生成配置和验证皮肤，最后由统一 macOS 引擎完成注入。

```text
图片 / 视频 / 网页需求
        ↓
Video Skill 或 Web Skin Builder
        ↓
theme.json + poster.jpg + background.mp4
        ↓
统一 macOS 引擎
        ↓
启动、验证、切换或恢复 Codex 皮肤
```

不要只复制 Skill、CSS 或单个素材。正常使用需要完整引擎。

## 二、安装

要求：

- macOS
- 官方 Codex Desktop 已安装并至少启动过一次
- 视频皮肤需要 `ffmpeg` 和 `ffprobe`

在项目目录运行：

双击 `Install Codex Skin Studio.command`，或使用命令行：

```bash
./scripts/install-dream-skin-macos.sh --no-launch
```

安装器会同时安装引擎和两个 Skill，并创建桌面启动、定制、验证和恢复入口。

## 三、使用视频 Skill

把视频交给 Codex，并说明“使用 codex-video-skin 将它做成视频皮肤”；也可以直接运行：

```bash
~/.codex/skills/codex-video-skin/scripts/apply_video_skin.sh --check
~/.codex/skills/codex-video-skin/scripts/apply_video_skin.sh "/视频绝对路径/video.mp4" "我的视频皮肤"
```

Skill 会移除音频、限制分辨率和体积、提取首帧海报、生成主题配置，并通过已验证的本机 Codex CDP 应用皮肤。

## 四、使用网页 Skill

告诉 Codex 使用 `codex-web-skin-builder` 创建网页皮肤。当前统一引擎支持：

- `default`
- `velorah`
- `mainframe`

生成后先验证：

```bash
python3 ~/.codex/skills/codex-web-skin-builder/scripts/validate_web_skin.py "/皮肤包绝对路径"
```

验证通过后运行皮肤包中的：

1. `安装物料.command`
2. `启动皮肤.command`

每个皮肤包只包含主题和预览文件，不包含自己的引擎副本。

## 五、直接使用内置皮肤

进入 `skins/video/` 或 `skins/web/`，打开目标皮肤目录，然后依次双击：

1. `安装物料.command`
2. `启动皮肤.command`

停止当前皮肤时双击 `停止皮肤.command`。

## 六、验证

```bash
~/.codex/codex-dream-skin-studio/scripts/doctor-macos.sh --require-live
~/.codex/codex-dream-skin-studio/scripts/verify-dream-skin-macos.sh --reload
```

只有验证成功并确认原生侧栏、任务内容、输入框仍可操作，才算皮肤应用完成。

## 七、恢复官方外观

双击桌面的 `Codex Skin Studio - Restore.command`，或运行：

```bash
~/.codex/codex-dream-skin-studio/scripts/restore-dream-skin-macos.sh \
  --restore-base-theme --restart-codex
```

恢复操作不会删除你的图片或皮肤包。

## 八、安全说明

- 不修改官方 Codex `.app`、`app.asar` 或签名。
- 不读取或改写 API Key、模型供应商和中转配置。
- CDP 只绑定本机 `127.0.0.1`，并校验监听进程属于官方 Codex。
- 皮肤素材保存在本机用户目录，目录权限为 `700`、文件权限为 `600`。
- 不要安装来源不明的皮肤脚本，也不要在皮肤启用期间运行不可信的本地程序。

## 九、GitHub 与 ZIP

这个目录本身就是 GitHub 上传目标，不包含旧仓库 Git 历史。发布 ZIP 由以下命令生成：

```bash
./scripts/build-release.sh
./scripts/build-client-release.sh "/输出目录/Codex-Skin-Studio-Complete.zip"
```

不要把 `node_modules`、`dist`、`.DS_Store`、运行日志或用户自己的主题素材提交到公开仓库。
